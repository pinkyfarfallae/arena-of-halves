# Use root path in development so dice/3D chunks load (no /arena-of-halves prefix).
# You must: 1) Restart dev server (npm start) after changing this. 2) Open http://localhost:3001/ (root only).
# Production build still uses homepage from package.json for GitHub Pages.
PUBLIC_URL=/
